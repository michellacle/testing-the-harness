# Feature: complete a todo

Add a `completeTodo(todos, id)` function to `src/todos.ts`.

- It returns a new array and marks only the todo matching `id` as completed.
- It must not mutate the input array or its todo objects.
- It throws an error when no todo has that ID.
- Add focused tests in `src/todos.test.ts`.
